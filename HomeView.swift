import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("SkillHive")
                .font(.largeTitle)
            Text("Book or Offer Peer Tutoring Sessions")
                .padding()
        }
    }
}
